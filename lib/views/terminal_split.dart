import 'package:flutter/material.dart';

class TerminalSplit extends StatelessWidget {
  const TerminalSplit({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
